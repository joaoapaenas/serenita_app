# Installation and Environment

This page describes how to set up and run Serenita from source.

## Running from Source

1.  **Prerequisites:**
    *   Python 3.10+
    *   `uv` (or `pip`)

2.  **Installation:**
    *   Clone the repository.
    *   Install dependencies: `uv pip install -r requirements.txt` (or equivalent)

3.  **Running the App:**
    *   Execute `python main.py` from the root directory.

## Packaged Installation (Future)

Instructions for running the packaged application will be available here once the PyInstaller build is finalized.
